NODE_ENV=development
PORT=3000

# Development database (SQLite via Prisma)
DATABASE_URL="file:./dev.db"

# Production database (PostgreSQL via Docker)
# DATABASE_URL="postgresql://postgres:CHANGE_ME_PASSWORD@db:5432/financial_system?schema=public"

# Docker Compose Postgres password (keep real value out of VCS)
POSTGRES_PASSWORD=CHANGE_ME_PASSWORD

